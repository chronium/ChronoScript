func foo(x) ret x * 3

func bar(x, y)
    if x < 10 do
        z = foo(x)
    else
        z = 5

    if y != 0 do
        y += foo(z)
        write(y)

bar(5, 10)

factorial(0) ret 1
factorial(n) ret n * factorial(n - 1)